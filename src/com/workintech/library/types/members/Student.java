package com.workintech.library.types.members;

import com.workintech.library.models.Member;

public class Student extends Member {

    public Student(long id, String name, int MAX_BOOKS) {
        super(id, name, MAX_BOOKS);
    }
}
